@model IEnumerable<CMS.Models.student>

@{
    ViewBag.Title = "Index";
}
   <font color="white"><br><br><br>
<h2>Student's Record</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<center>
    
    <table border="3">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)

        </th>
        <th>
           <pre><pre>    @Html.DisplayNameFor(model => model.password)
     </th>
        <th>
           <pre>    @Html.DisplayNameFor(model => model.clas)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
                        @Html.DisplayFor(modelItem => item.name)

        </td>
        <td>

                      <pre>       @Html.DisplayFor(modelItem => item.password)
        </td>
        <td>
           <pre>           @Html.DisplayFor(modelItem => item.clas)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table></center></font>
